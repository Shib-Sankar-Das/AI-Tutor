# Agentic AI Tutor - Environment Variables
# Copy this file to .env.local and fill in your values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google Gemini API
GOOGLE_API_KEY=your_google_api_key

# Google OAuth Configuration (for Google Calendar, Slides, Docs integration)
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable these APIs: Google Calendar API, Google Slides API, Google Docs API, Google Drive API
# 4. Go to "Credentials" > "Create Credentials" > "OAuth client ID"
# 5. Set application type to "Web application"
# 6. Add authorized redirect URIs:
#    - http://localhost:3000/auth/callback (for development)
#    - https://your-domain.com/auth/callback (for production)
# 7. Copy Client ID to GOOGLE_CLIENT_ID below
# 8. In Supabase Dashboard, go to Authentication > Providers > Google
#    - Enable Google provider
#    - Add Client ID and Client Secret from Google Cloud Console

# Note: The actual OAuth flow is handled by Supabase Auth
# These environment variables are for reference and frontend use

# Application URL (for CORS and OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000